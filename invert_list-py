""" def reverse_list(l):
    new_list = []
    index_list = len(l) -1
    
    while index_list >= 0:
        new_list.append(l[index_list])
        index_list = index_list -1
 """


def reverse_list(l):
  #'return a list with the reverse order of l'
  re = []
  for i in range(0, len(l)):
    re.append(l.pop())
  return re
    
s = [1,2,3,4]
print(reverse_list(s))